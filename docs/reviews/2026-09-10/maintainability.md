# Klexi maintainability review — 2026-09-10 (원문)

Scope: `klexi_src` @ `38cdcf2` (1.0.6+52), local Flutter 3.41.4. Verified by running `flutter analyze`, `dart format --set-exit-if-changed`, `flutter pub outdated`, `npm ls/audit`.

**1. HIGH — CI cannot pass, three independent ways.** `.github/workflows/ci.yml:20,61` pins Flutter 3.29.0, but `pubspec.lock:1305-1306` requires Dart ≥3.10.3 / Flutter ≥3.38.4 → `flutter pub get` fails. Even with the pin fixed: `dart format --set-exit-if-changed lib/` exits 1 (84 of 92 files unformatted), and `flutter analyze --no-fatal-infos` exits 1 (2 warnings: `pubspec.yaml:86-87` declares `assets/images/`, `assets/icons/` which don't exist). `ci.yml:73` also decodes the keystore into `android/keystore/`, a directory that doesn't exist (real jks is at `KlexiDev\keystore\`; `android/key.properties:4` points to `../klexi-release.jks`). The README's CI badge is advertising a green build that never ran. *Fix:* pin `3.41.4`, run `dart format lib/` once, delete the two asset lines, fix the keystore path.

**2. HIGH — Premium-gating bug regressed in 4 screens (the build48 #19 bug).** CLAUDE.md:124/243 says always pass `isPremium, userLevel`; only `daily_session_screen.dart:34` and `sentence_card_screen.dart:87` do. `quiz_screen.dart:69`, `review_screen.dart:62`, `sentence_practice_screen.dart:57`, `pronunciation_screen.dart:60` call `getTodayWordIds()`/`getTodaySession()` with defaults → paying users get level-1 filler. Root cause: optional params (`daily_session_service.dart:107,217`). *Fix:* make both `required`; the compiler finds every call site.

**3. HIGH — SRS: docs, tests and code describe three different algorithms.** ADR-004 and README say "SM-2". The real scheduler is a fixed table `[1,3,7,14,30,60]` keyed on `timesStudied` (`daily_session_service.dart:40-47`). `lib/core/utils/srs_algorithm.dart` (95 lines, true SM-2 with ease factors) is imported nowhere — dead. `test/sm2_algorithm_test.dart` re-implements SM-2 inline and tests that, not the app. `recordReview()` maps quality→bool at `:223`, discarding the SM-2 scale. *Fix:* delete `srs_algorithm.dart`, rewrite the test against `StudyRecord.intervalDays`/`isDueToday` (inject a clock), fix ADR-004 wording.

**4. HIGH — Polar remnants still live.** `polar_service.dart` (a 3-line `export`) is imported by `review_screen.dart:7`, `sentence_practice_screen.dart:14`, `pronunciation_screen.dart:9`, `level_words_screen.dart:10`, `word_network_screen.dart:15` — CHANGELOG (build46 "Removed") and CLAUDE.md #17 both claim this is done. Backend: `functions/src/polar.ts` (156 lines, not wired in `index.ts`) still compiles to `lib/polar.js`; `@polar-sh/sdk` remains in `package.json:15`; `package.json:3` still says "Polar payments". *Fix:* 5 import swaps, delete both files, `npm uninstall @polar-sh/sdk`.

**5. HIGH — Tests mostly test copies, not code.** 6 of 11 files (`dalli_chat_test`, `sm2_algorithm_test`, `tts_service_test`, `notification_service_test`, half of `pronunciation_service_test`) define private helper functions inside the test and assert on those. They pass forever while the app breaks. Only `word_repository_test`, `word_model_test`, `grammar_content_test`, `auth_service_test` (constructor only), `daily_session_service_test` (`StudyRecord` only) touch real code; all still compile against current signatures. Zero coverage of: `deleteAccount()` (`auth_service.dart:98`), session selection (`getTodaySession`), purchase gating (`purchase_service.dart:56`), `PaywallGate`. `mockito` is a dev-dep used nowhere. *Fix (PM 정정 D-14):* 일괄 삭제가 아니라 한 건씩 판단 — `grammar_content_test`가 실결함 74건을 잡음.

**6. MED — `flutter test` hang.** Only `widget_test.dart` needs rendering (`testWidgets`, a placeholder `expect(true,isTrue)`); every other file is plain `test()`. But `flutter_test` boots `flutter_tester` per file regardless, and `dart test` is impossible because `word.dart:1` extends `HiveObject` (pulls Flutter) and services import `flutter/foundation`. (PM 확정 D-15: CI ubuntu에서는 90건 2m23s 정상 → 테스트는 CI에서만.)

**7. MED — Singletons and Riverpod interleaved.** `DailySessionService.instance`, `PurchaseService.instance`, `AnalyticsService.instance` (7 direct uses), `WordRepository.instance` are wrapped by providers (`daily_session_service.dart:11`, `purchase_service.dart:99`) but also called statically (`quiz_screen.dart:129`, `main.dart:36-43`). `PurchaseService.attachNotifier` (`:33`) is a manual back-link that a `late` field makes crash-prone. 20 screens are `StatefulWidget` + `setState` holding business state. *Fix:* construct services inside providers; drop `.instance`.

**8. MED — Business logic inside screens.** Quiz generation (`quiz_screen.dart:54-106`), review selection (`review_screen.dart:40-79`), session filtering repeated in 4 screens (`(isPremium || w.level == 1)`). Untestable without widgets. *Fix:* a `QuizBuilder`/`SessionSelector` in `core/` returning plain data.

**9. MED — Paywall UI duplicated 4×.** `PaywallGate` (`paywall_gate.dart:39`), `_showUpgradeDialog` (`word_network_screen.dart:345`), inline "Pro Only" block (`level_words_screen.dart:121-145`), `_PremiumSheet` (`settings_screen.dart:446` — its button just pops, never navigates to `/premium`). *Fix:* one `showUpgradePrompt(context)`.

**10. MED — TOPIK badge copy-pasted in 13 sites/12 files** (`home_screen.dart:357,482`, `learn_screen.dart:228`, `quiz_screen.dart:414`, `review_screen.dart:224`, `daily_session_screen.dart:146`, `level_words_screen.dart:201`, `grammar_detail_screen.dart:47`, `word_card_screen.dart:91`, `word_network_screen.dart:1021`, `sentence_card_screen.dart:244`, `sentence_practice_screen.dart:194`, `progress_screen.dart:25`). 119 raw `Color(0x…)` literals outside `AppColors` (`settings_screen` 40, `progress_screen` 25) — `0xFFFF8C42`/`0xFF667EEA` are `AppColors.primary` re-typed. 91 deprecated `withOpacity`. *Fix:* `core/widgets/topik_badge.dart`; sed literals → `AppColors`.

**11. MED — Docs drift.** CLAUDE.md:37/222/230 use `C:/KlexiDev/` (real: `C:\workspace\klexi\KlexiDev`); CLAUDE.md:12 says build51, README:14 says +49, pubspec is +52; CHANGELOG stops at +48 (49–52 undocumented, including the account-deletion rewrite); README:151 says `firebase_options.dart` is git-excluded — correct (`.gitignore:4`) but then CI's `flutter analyze` cannot compile `main.dart:15`. ANDROIDP_POLICY §7: A (mobile TTS I/O is already async, `tts_service_mobile.dart:22-34`), B (`catch(e,st)` + Crashlytics at `tts_service.dart:96,127`), D (localhost gated by `FUNCTIONS_EMULATOR`, `index.ts:16`), F (`notification_service.dart:42`) are all resolved; C is still open (12 moderate npm advisories). *Fix:* one honest pass; add build49–52 to CHANGELOG.

**12. MED — Dependencies.** `flutter_markdown 0.7.7+1` is **discontinued** (replaced by `flutter_markdown_plus`). Majors pending: `purchases_flutter` 9.15→10.12, `flutter_riverpod` 2.6→3.4 (StateNotifier deprecated), `timezone` 0.9→0.11, `record` 6→7, `go_router` 14→18, all Firebase plugins +1 major, `flutter_local_notifications` 18→22. All `^` ranges; lock is source of truth. `functions/package.json:16` has `@types/multer` in `dependencies`; `engines.node="22"` while local is v24. *Fix:* swap markdown package first; do Firebase + RevenueCat majors together on a branch.

**13. LOW — Config hygiene.** `app_config.dart:7` hardcodes `backendUrl` (no `String.fromEnvironment`), `:13` `isBackendConfigured => true` is dead. Settings shows literal `'1.0.0'` (`settings_screen.dart:99`) while app is 1.0.6+52; `package_info_plus` not in pubspec. `analysis_options.yaml` only adds 2 rules; `avoid_catches_without_on_clauses` is violated at `main.dart:28`, `review_screen.dart:68`.

**14. LOW — God-files.** `word_network_screen.dart` 1,167 lines, `settings_screen.dart` 691, `dalli_chat_screen.dart` 592 (holds 3 global providers at `:70-78`), `home_screen.dart` 557. Content data files (`grammar_data.dart` 1,740, `sentence_groups_l*` 1,685) are fine as data.

**15. LOW — Cross-feature imports:** none found (good); `app_router.dart:6-28` is the only feature aggregator.

## Recommended cleanup order
1. Make CI green (#1). 2. `required` params (#2). 3. Remove Polar + dead SRS + (한 건씩 판단한) 가짜 테스트 + mockito (#3, #4, #5). 4. Docs truth pass (#11). 5. Extract `TopikBadge` / upgrade prompt / quiz builder, then dependency majors (#8–10, #12).
